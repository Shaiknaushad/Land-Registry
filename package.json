{
  "name": "land-registration",
  "version": "1.0.0",
  "description": "Blockchain Land Registration Project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.20.0"
  }
}